#define CC_QUALITY_HIGH 0.
#define CC_QUALITY_MEDIUM 1.
#define CC_QUALITY_LOW 2.

#pragma builtin(global)
layout(set = 0, binding = 8) uniform CCCustomGlobal {
    vec4 cc_render_setting;     // x: quality, vertex culling distance
    vec4 cc_sky_camera_pos;
    vec4 cc_window_size;
};

